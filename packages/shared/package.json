{
  "name": "@idle/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Общий игровой движок: конфиги, контент, формулы, детерминированный симулятор боя, действия игрока",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "balance": "tsx scripts/balance-sim.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.20.0",
    "typescript": "5.9.3",
    "vitest": "^3.2.4"
  }
}
